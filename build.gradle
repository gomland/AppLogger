// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.1.2'
        

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}
task archives(description: 'Create a deployment package.', type: Copy) {
    println('Create archives.')

    into("libs") {
        from "appLogger/build/outputs/aar/appLogger-release.aar"
        rename { fileName ->
            fileName.replace("release", "${project.LOGGER_VERSION}")
        }
    }

    destinationDir file("$project.LOGGER_ARCHIVES_DIR")
}

task clean(type: Delete) {
    delete rootProject.buildDir
    delete "$project.LOGGER_ARCHIVES_DIR"
}